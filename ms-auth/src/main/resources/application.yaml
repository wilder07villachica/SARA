server:
  port: ${PORT:8080}

spring:
  application:
    name: ms-auth

  datasource:
    url: jdbc:mysql://localhost:3306/ms_auth
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true